# V2026.09.15.01: HL PDF authentication and Reclass actions

The original PDF worker rejected Apps Script with HTTP 403 because it required the caller bearer to exactly equal the Edge environment key. The worker now sends the caller credentials to the existing service-only `hl_po_import_capabilities` RPC. PostgREST verifies authentication and its execute grant before the worker reads PDF content. The same caller credentials stage every page; the worker never substitutes its own service credential. Authentication outages return a retryable 503, while unauthorized callers receive 403. Modern secret keys use the apikey header. This needs no schema or Apps Script changes.

Reclass shows its four item-wide Hold/Stop choices above every action view. They share the existing action handler, keep affected-row counts visible, and synchronize the reason with the expanded originating row. One Hold/Stop choice can accompany Priority, Move Up, Move Down, Re-Count, and location detail requests. Existing season/year scope, off-action code eligibility, quantity validation, and protected delivery remain in force. Shared Eval Work retains its layout.

Before publishing, run the Edge authentication tests, focused Reclass unit/browser checks against a fresh compiled shell, and the HL season SQL test including the service-only probe ACL. Then use the additional authorized exact-commit candidate validation and existing hosted gates. Deploy the validated worker before the frontend. Verify a real Apps Script PDF import completes staging, and retain the active report until Dylan confirms the Chicago receipt cutoff. A green frontend deployment alone does not demonstrate PDF import success.
